<%- include('./partials/header.ejs') %> 
<%- include('./partials/nav.ejs') %>
<h1><%= title %></h1>
<p>
  this is a dummy text.this is a dummy text.this is a dummy text.this is a dummy
  text.this is a dummy text.
</p>
<%= new Date().toString() %> <% if(post.published) { %>
<h3>Title:<%= post.title %></h3>
<p>Body: <%=post.body %></p>

<% } else{ %>
<p>Post is not yet published</p>

<% } %>

<ul>
  <% for(let p of posts) { %>
  <li><%= p.title %> By <%= p.author %></li>
  <% } %>
</ul>
<%- include('./partials/footer.ejs') %>
